part of 'layout.dart';

class PersistedDrawer extends StatelessWidget {
  const PersistedDrawer({
    Key? key,
    required this.drawer,
  }) : super(key: key);

  final Widget drawer;

  @override
  Widget build(BuildContext context) {
    return drawer;
  }
}
